<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Albania — Country Profile (Expanded)</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    :root{
      --accent:#b30000;
      --bg:#f0f0f5;
      --card:#ffffff;
      --text:#1b1b1b;
      --muted:#555;
      --card-shadow: 0 8px 24px rgba(0,0,0,0.16);
    }
    *{box-sizing:border-box}
    body{
      font-family:"Segoe UI", Roboto, Arial, sans-serif;
      margin:0;
      background:var(--bg);
      color:var(--text);
      line-height:1.5;
    }

    /* Header */
    header{
      background:linear-gradient(90deg,var(--accent),#8b0000);
      color:#fff;
      padding:24px 16px;
      text-align:left;
      display:flex;
      gap:16px;
      align-items:center;
    }
    header .flag{
      width:88px;
      height:60px;
      border-radius:8px;
      border:3px solid rgba(255,255,255,0.15);
      background:#fff;
      display:inline-block;
      overflow:hidden;
    }
    header h1{
      font-size:28px;
      margin:0;
      letter-spacing:0.4px;
    }
    header p{
      margin:4px 0 0 0;
      opacity:0.95;
      font-size:14px;
    }

    /* Layout */
    .wrap{
      width:94%;
